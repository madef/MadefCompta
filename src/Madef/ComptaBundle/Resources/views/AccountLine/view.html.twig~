{% extends "MadefComptaBundle:Layout:default.twig" %}
{% block title %}Détail de {% endblock %}
{% block content %}
	<h1>Réservation de la chambre #{{ reservation.getRoom() }} du {{ reservation.getIncomeDate("j M. Y") }} au {{ reservation.getOutcomeDate("j M. Y") }}</h1>
	<table class="table table-striped table-hover">
		<tbody>
			<tr>
				<td>Nom</td>
				<td>{{ reservation.getCustomerName() }}</td>
			</tr>
			<tr>
				<td>Dates</td>
				<td>{{ reservation.getIncomeDate("j M. Y") }} au {{ reservation.getOutcomeDate("j M. Y") }}</td>
			</tr>
			<tr>
				<td>Numéro de la chambre</td>
				<td>{{ reservation.getRoom() }}</td>
			</tr>
			<tr>
				<td>Nombre de personne</td>
				<td>{{ reservation.getNbPers() }}</td>
			</tr>
			<tr>
				<td>Nombre d'enfant</td>
				<td>{{ reservation.getNbChilds() }}</td>
			</tr>
			<tr>
				<td>Téléphone</td>
				<td>{{ reservation.getPhoneNumber() }}</td>
			</tr>
			<tr>
				<td>Email</td>
				<td>{{ reservation.getEmail() }}</td>
			</tr>
			<tr>
				<td>Commentaire</td>
				<td>{{ reservation.getComment() }}</td>
			</tr>
			<tr>
				<td>Source</td>
				<td>{{ reservation.getSrc() }}</td>
			</tr>
		</tbody>
	</table>
	<a class="btn btn-large btn-primary" type="button" href="{{ url('edit', {reservation: reservation.getId()}) }}">Modifier</a>
{% endblock %}
